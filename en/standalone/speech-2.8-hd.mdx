---
title: "speech-2.8-hd"
description: "Use MiniMax speech-2.8-hd for text-to-speech through the baze platform unified API."
openapi: "/api-reference/en/zmodelAudio/minimax/speech-2.8-hd.json POST /v1/audio/speech"
hideFooterPagination: true
---

Use the baze platform unified API at `/v1/audio/speech` to synthesize speech with `speech-2.8-hd`.

This is the standalone entry page for `speech-2.8-hd`. It reuses the same public API definition as the page under the Model APIs menu and documents only the fields and response behavior currently exposed by the platform API. It does not represent the complete MiniMax upstream capability set.
