---
title: 'PowerTokens 新用户快速入门'
description: '完成注册、充值，并按推荐路径接入 PowerTokens。包含 API Key 创建、模型调用、日志和用量统计的完整图文指南。'
mode: 'custom'
icon: 'bolt'
iconType: 'solid'
---

<style>{`
  @import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&family=JetBrains+Mono:wght@400;500;600&family=Noto+Sans+SC:wght@300;400;500;600;700;800;900&display=swap');

  :root {
    --primary: #6366f1;
    --primary-light: #818cf8;
    --primary-dark: #4f46e5;
    --accent: #06b6d4;
    --accent-light: #22d3ee;
    --success: #10b981;
    --warning: #f59e0b;
    --danger: #ef4444;
    --bg: #0f172a;
    --bg-card: #1e293b;
    --bg-card-hover: #273549;
    --bg-code: #0d1117;
    --text: #e2e8f0;
    --text-secondary: #94a3b8;
    --text-muted: #64748b;
    --border: #334155;
    --border-light: #475569;
    --shadow: 0 4px 6px -1px rgba(0,0,0,.3), 0 2px 4px -2px rgba(0,0,0,.3);
    --shadow-lg: 0 10px 15px -3px rgba(0,0,0,.4), 0 4px 6px -4px rgba(0,0,0,.4);
    --radius: 12px;
    --radius-sm: 8px;
    --radius-lg: 16px;
    --transition: 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  }

  * { margin: 0; padding: 0; box-sizing: border-box; }

  html { scroll-behavior: smooth; }

  body {
    font-family: 'Inter', 'Noto Sans SC', -apple-system, sans-serif;
    background: var(--bg) !important;
    color: var(--text);
    line-height: 1.7;
    overflow-x: hidden;
  }

  .guide-root {
    position: relative;
    min-height: 100vh;
    padding-top: 16px;
  }

  /* ─── Animated Background ─── */
  .bg-grid {
    position: fixed; inset: 0; z-index: -1;
    background-image:
      linear-gradient(rgba(99,102,241,.03) 1px, transparent 1px),
      linear-gradient(90deg, rgba(99,102,241,.03) 1px, transparent 1px);
    background-size: 60px 60px;
    mask-image: radial-gradient(ellipse 80% 50% at 50% 0%, #000 40%, transparent 80%);
    pointer-events: none;
  }
  .bg-glow {
    position: fixed; z-index: -1;
    border-radius: 50%; filter: blur(120px);
    opacity: .15; pointer-events: none;
  }
  .bg-glow.g1 { width: 600px; height: 600px; background: var(--primary); top: -200px; left: -100px; }
  .bg-glow.g2 { width: 500px; height: 500px; background: var(--accent); top: 40%; right: -150px; }
  .bg-glow.g3 { width: 400px; height: 400px; background: #a855f7; bottom: -100px; left: 30%; }

  /* ─── Layout ─── */
  .layout { display: flex; max-width: 1440px; margin: 0 auto; position: relative; }

  /* ─── Sidebar ─── */
  .sidebar {
    position: sticky; top: 64px; width: 260px; min-width: 260px;
    height: calc(100vh - 64px); overflow-y: auto;
    padding: 32px 20px; border-right: 1px solid var(--border);
    background: rgba(15,23,42,.5); backdrop-filter: blur(8px);
  }
  .sidebar::-webkit-scrollbar { width: 4px; }
  .sidebar::-webkit-scrollbar-thumb { background: var(--border); border-radius: 4px; }
  .sidebar h3 { font-size: .7rem; text-transform: uppercase; letter-spacing: .1em; color: var(--text-muted); margin-bottom: 12px; font-weight: 600; }
  .sidebar .toc { list-style: none; }
  .sidebar .toc li { margin-bottom: 2px; }
  .sidebar .toc a {
    display: block; padding: 7px 12px;
    color: var(--text-secondary); text-decoration: none;
    font-size: .85rem; border-radius: 6px;
    transition: var(--transition); border-left: 2px solid transparent;
  }
  .sidebar .toc a:hover, .sidebar .toc a.active {
    color: var(--primary-light);
    background: rgba(99,102,241,.1);
    border-left-color: var(--primary);
  }
  .sidebar .toc a.sub { padding-left: 28px; font-size: .8rem; }
  .sidebar .progress-ring {
    margin-top: 28px; padding: 16px;
    background: var(--bg-card); border-radius: var(--radius);
    text-align: center; border: 1px solid var(--border);
  }
  .sidebar .progress-ring .ring-label { font-size: .75rem; color: var(--text-muted); margin-bottom: 4px; }
  .sidebar .progress-ring .ring-value {
    font-size: 1.6rem; font-weight: 700;
    background: linear-gradient(135deg, var(--primary-light), var(--accent-light));
    -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text;
  }

  /* ─── Main Content ─── */
  .main { flex: 1; min-width: 0; padding: 48px 56px 80px; }

  /* ─── Hero Section ─── */
  .hero { margin-bottom: 56px; position: relative; }
  .hero .badge {
    display: inline-flex; align-items: center; gap: 6px;
    padding: 5px 14px; border-radius: 20px;
    font-size: .8rem; font-weight: 500;
    background: rgba(99,102,241,.15); color: var(--primary-light);
    border: 1px solid rgba(99,102,241,.3); margin-bottom: 20px;
  }
  .hero .badge .dot { width: 7px; height: 7px; border-radius: 50%; background: var(--success); animation: pulse-dot 2s ease-in-out infinite; }
  @keyframes pulse-dot { 0%, 100% { opacity: 1; } 50% { opacity: .4; } }
  .hero h1 {
    font-size: clamp(2rem, 5vw, 3.2rem); font-weight: 800;
    letter-spacing: -.02em; line-height: 1.2; margin-bottom: 16px;
    background: linear-gradient(135deg, #e2e8f0 0%, #a5b4fc 50%, #67e8f9 100%);
    -webkit-background-clip: text; -webkit-text-fill-color: transparent; background-clip: text;
  }
  .hero .subtitle { font-size: 1.15rem; color: var(--text-secondary); max-width: 640px; line-height: 1.7; }

  /* ─── Step Cards ─── */
  .step-section { margin-bottom: 24px; scroll-margin-top: 80px; }
  .step-section .step-header {
    display: flex; align-items: flex-start; gap: 16px;
    cursor: pointer; padding: 24px 28px;
    background: var(--bg-card); border-radius: var(--radius-lg);
    border: 1px solid var(--border); transition: var(--transition); user-select: none;
  }
  .step-section .step-header:hover {
    background: var(--bg-card-hover); border-color: var(--border-light);
    box-shadow: var(--shadow-lg); transform: translateY(-1px);
  }
  .step-section .step-number {
    width: 44px; height: 44px; min-width: 44px; border-radius: 12px;
    display: flex; align-items: center; justify-content: center;
    font-weight: 700; font-size: 1.2rem;
    background: linear-gradient(135deg, var(--primary-dark), var(--primary));
    color: #fff; box-shadow: 0 0 20px rgba(99,102,241,.3);
  }
  .step-section.open .step-number {
    background: linear-gradient(135deg, var(--primary), var(--accent));
    box-shadow: 0 0 30px rgba(99,102,241,.5);
  }
  .step-section .step-info { flex: 1; }
  .step-section .step-info h2 { font-size: 1.25rem; font-weight: 700; margin-bottom: 4px; color: var(--text); }
  .step-section .step-info p { color: var(--text-secondary); font-size: .9rem; }
  .step-section .step-arrow {
    font-size: 1.5rem; color: var(--text-muted);
    transition: transform var(--transition); min-width: 24px; text-align: center;
  }
  .step-section.open .step-arrow { transform: rotate(180deg); color: var(--primary-light); }
  .step-section .step-body {
    max-height: 0; overflow: hidden;
    transition: max-height .5s cubic-bezier(0.4, 0, 0.2, 1), margin .3s;
  }
  .step-section.open .step-body { max-height: 12000px; margin-top: 16px; }
  .step-section .step-content {
    background: var(--bg-card); border: 1px solid var(--border);
    border-radius: var(--radius-lg); padding: 32px;
  }

  /* ─── Cards Grid ─── */
  .cards-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(280px, 1fr)); gap: 16px; margin-bottom: 24px; }
  .card {
    background: rgba(30,41,59,.6); border: 1px solid var(--border);
    border-radius: var(--radius); padding: 24px;
    transition: var(--transition); position: relative; overflow: hidden;
  }
  .card::before {
    content: ''; position: absolute; top: 0; left: 0; right: 0; height: 2px;
    background: linear-gradient(90deg, var(--primary), var(--accent));
    opacity: 0; transition: opacity var(--transition);
  }
  .card:hover { background: var(--bg-card-hover); border-color: var(--border-light); transform: translateY(-2px); box-shadow: var(--shadow-lg); }
  .card:hover::before { opacity: 1; }
  .card .card-icon {
    width: 40px; height: 40px; border-radius: 10px;
    display: flex; align-items: center; justify-content: center;
    font-size: 1.3rem; margin-bottom: 12px;
  }
  .card .card-icon.primary { background: rgba(99,102,241,.15); }
  .card .card-icon.accent { background: rgba(6,182,212,.15); }
  .card .card-icon.success { background: rgba(16,185,129,.15); }
  .card .card-icon.warning { background: rgba(245,158,11,.15); }
  .card h4 { font-size: 1rem; font-weight: 600; margin-bottom: 6px; color: var(--text); }
  .card p { font-size: .85rem; color: var(--text-secondary); line-height: 1.6; }

  /* ─── Screenshot / Image ─── */
  .screenshot-wrap {
    margin: 24px 0;
    border-radius: var(--radius-lg);
    overflow: hidden;
    border: 1px solid var(--border);
    background: var(--bg-code);
    cursor: zoom-in;
    transition: var(--transition);
    position: relative;
  }
  .screenshot-wrap:hover {
    border-color: var(--border-light);
    box-shadow: var(--shadow-lg);
  }
  .screenshot-wrap:hover .screenshot-img {
    transform: scale(1.02);
  }
  .screenshot-wrap .screenshot-img {
    display: block;
    width: 100%;
    height: auto;
    transition: transform .4s ease;
  }
  .screenshot-wrap .screenshot-overlay {
    position: absolute;
    inset: 0;
    background: rgba(0,0,0,0);
    transition: background .3s ease;
    display: flex; align-items: center; justify-content: center;
    pointer-events: none;
  }
  .screenshot-wrap:hover .screenshot-overlay {
    background: rgba(0,0,0,.35);
  }
  .screenshot-wrap .screenshot-overlay .zoom-icon {
    opacity: 0;
    transform: scale(.8);
    transition: all .3s ease;
    background: rgba(255,255,255,.15);
    backdrop-filter: blur(4px);
    width: 48px; height: 48px;
    border-radius: 50%;
    display: flex; align-items: center; justify-content: center;
    font-size: 1.4rem;
    color: #fff;
  }
  .screenshot-wrap:hover .screenshot-overlay .zoom-icon {
    opacity: 1;
    transform: scale(1);
  }
  .screenshot-caption {
    margin-top: 10px;
    text-align: center;
    font-size: .8rem;
    color: var(--text-muted);
    display: flex; align-items: center; justify-content: center;
    gap: 6px;
  }
  .screenshot-caption .caption-dot {
    width: 5px; height: 5px;
    border-radius: 50%;
    background: var(--primary-light);
  }

  /* ─── Lightbox ─── */
  .lightbox {
    display: none;
    position: fixed;
    inset: 0;
    z-index: 9999;
    background: rgba(0,0,0,.92);
    backdrop-filter: blur(8px);
    cursor: zoom-out;
    align-items: center;
    justify-content: center;
    padding: 40px;
  }
  .lightbox.open { display: flex; }
  .lightbox img {
    max-width: 95vw;
    max-height: 90vh;
    border-radius: var(--radius);
    box-shadow: 0 20px 60px rgba(0,0,0,.6);
    object-fit: contain;
    animation: lightboxIn .35s ease;
  }
  @keyframes lightboxIn {
    from { opacity: 0; transform: scale(.9); }
    to { opacity: 1; transform: scale(1); }
  }
  .lightbox .lb-close {
    position: fixed;
    top: 20px; right: 28px;
    width: 44px; height: 44px;
    border-radius: 50%;
    background: rgba(255,255,255,.1);
    border: 1px solid rgba(255,255,255,.2);
    color: #fff;
    font-size: 1.4rem;
    cursor: pointer;
    display: flex; align-items: center; justify-content: center;
    transition: var(--transition);
    z-index: 10000;
  }
  .lightbox .lb-close:hover { background: rgba(255,255,255,.2); }
  .lightbox .lb-nav {
    position: fixed; top: 50%; transform: translateY(-50%);
    width: 50px; height: 50px; border-radius: 50%;
    background: rgba(255,255,255,.1); border: 1px solid rgba(255,255,255,.2);
    color: #fff; font-size: 1.4rem; cursor: pointer;
    display: flex; align-items: center; justify-content: center;
    transition: var(--transition); z-index: 10000;
  }
  .lightbox .lb-nav:hover { background: rgba(255,255,255,.2); }
  .lightbox .lb-prev { left: 24px; }
  .lightbox .lb-next { right: 24px; }
  .lightbox .lb-counter {
    position: fixed; bottom: 24px; left: 50%; transform: translateX(-50%);
    color: rgba(255,255,255,.6); font-size: .85rem;
    background: rgba(0,0,0,.5); padding: 6px 16px;
    border-radius: 20px; z-index: 10000;
  }

  /* ─── Code Blocks ─── */
  .code-block-wrapper {
    margin: 20px 0; border-radius: var(--radius); overflow: hidden;
    border: 1px solid var(--border); background: var(--bg-code);
  }
  .code-block-wrapper .code-header {
    display: flex; align-items: center; justify-content: space-between;
    padding: 10px 16px; background: rgba(255,255,255,.03);
    border-bottom: 1px solid rgba(255,255,255,.06);
    font-size: .8rem; color: var(--text-muted);
  }
  .code-block-wrapper .code-header .lang-tag { font-family: 'JetBrains Mono', monospace; font-size: .75rem; padding: 2px 8px; border-radius: 4px; background: rgba(99,102,241,.2); color: var(--primary-light); }
  .copy-btn { background: none; border: 1px solid var(--border); color: var(--text-muted); padding: 4px 10px; border-radius: 4px; font-size: .75rem; cursor: pointer; transition: var(--transition); font-family: 'Inter', sans-serif; }
  .copy-btn:hover { color: var(--text); border-color: var(--border-light); }
  .copy-btn.copied { color: var(--success); border-color: var(--success); }
  pre { padding: 20px; overflow-x: auto; font-family: 'JetBrains Mono', 'Fira Code', monospace; font-size: .85rem; line-height: 1.7; color: #c9d1d9; margin: 0; }
  pre::-webkit-scrollbar { height: 6px; }
  pre::-webkit-scrollbar-thumb { background: rgba(255,255,255,.1); border-radius: 3px; }

  .kw  { color: #ff7b72; }
  .str { color: #a5d6ff; }
  .cm  { color: #8b949e; font-style: italic; }
  .fn  { color: #d2a8ff; }
  .num { color: #79c0ff; }
  .op  { color: #ff7b72; }
  .var { color: #ffa657; }
  .url { color: #7ee787; }

  /* ─── Info Boxes ─── */
  .info-box { margin: 20px 0; padding: 16px 20px; border-radius: var(--radius); border-left: 3px solid; display: flex; gap: 12px; align-items: flex-start; font-size: .9rem; line-height: 1.6; }
  .info-box .info-icon { font-size: 1.2rem; min-width: 24px; text-align: center; }
  .info-box.tip { background: rgba(16,185,129,.08); border-color: var(--success); color: #6ee7b7; }
  .info-box.warning-box { background: rgba(245,158,11,.08); border-color: var(--warning); color: #fcd34d; }
  .info-box.info { background: rgba(99,102,241,.08); border-color: var(--primary); color: #a5b4fc; }
  .info-box.danger { background: rgba(239,68,68,.08); border-color: var(--danger); color: #fca5a5; }

  /* ─── Table ─── */
  .styled-table { width: 100%; border-collapse: collapse; margin: 20px 0; font-size: .9rem; }
  .styled-table th { text-align: left; padding: 12px 16px; background: rgba(99,102,241,.1); color: var(--primary-light); font-weight: 600; font-size: .8rem; text-transform: uppercase; letter-spacing: .05em; border-bottom: 2px solid var(--border); }
  .styled-table td { padding: 12px 16px; border-bottom: 1px solid var(--border); color: var(--text-secondary); }
  .styled-table tbody tr:hover td { background: rgba(255,255,255,.02); color: var(--text); }

  /* ─── Timeline ─── */
  .timeline { position: relative; padding-left: 28px; margin: 24px 0; }
  .timeline::before { content: ''; position: absolute; left: 8px; top: 0; bottom: 0; width: 2px; background: linear-gradient(180deg, var(--primary), var(--accent), transparent); }
  .timeline .tl-item { position: relative; margin-bottom: 24px; padding-left: 24px; }
  .timeline .tl-item::before { content: ''; position: absolute; left: -22px; top: 6px; width: 10px; height: 10px; border-radius: 50%; background: var(--primary); box-shadow: 0 0 0 3px rgba(99,102,241,.2); }
  .timeline .tl-item:nth-child(2)::before { background: var(--accent); box-shadow: 0 0 0 3px rgba(6,182,212,.2); }
  .timeline .tl-item:nth-child(3)::before { background: var(--success); box-shadow: 0 0 0 3px rgba(16,185,129,.2); }
  .timeline .tl-item h4 { font-size: 1rem; font-weight: 600; margin-bottom: 4px; }
  .timeline .tl-item p { font-size: .875rem; color: var(--text-secondary); }

  /* ─── Tags ─── */
  .tag { display: inline-block; padding: 3px 10px; border-radius: 12px; font-size: .75rem; font-weight: 500; margin: 2px 4px 2px 0; }
  .tag.api { background: rgba(99,102,241,.2); color: var(--primary-light); }
  .tag.model { background: rgba(6,182,212,.2); color: var(--accent-light); }
  .tag.billing { background: rgba(16,185,129,.2); color: #6ee7b7; }
  .tag.security { background: rgba(239,68,68,.2); color: #fca5a5; }

  /* ─── Progress Bar ─── */
  .progress-track { height: 6px; background: var(--border); border-radius: 3px; margin: 16px 0; overflow: hidden; }
  .progress-fill { height: 100%; border-radius: 3px; background: linear-gradient(90deg, var(--primary), var(--accent)); transition: width 1s cubic-bezier(0.4, 0, 0.2, 1); }

  /* ─── Footer ─── */
  .guide-footer { margin-top: 64px; padding: 40px 0 0; border-top: 1px solid var(--border); }
  .guide-footer .footer-nav { display: grid; grid-template-columns: 1fr 1fr; gap: 16px; margin-bottom: 24px; }
  .guide-footer .footer-card { padding: 20px; background: var(--bg-card); border: 1px solid var(--border); border-radius: var(--radius); text-decoration: none; transition: var(--transition); display: flex; align-items: center; gap: 12px; }
  .guide-footer .footer-card:hover { background: var(--bg-card-hover); border-color: var(--border-light); transform: translateY(-2px); }
  .guide-footer .footer-card .fc-arrow { color: var(--text-muted); font-size: 1.2rem; }
  .guide-footer .footer-card .fc-info { flex: 1; }
  .guide-footer .footer-card .fc-label { font-size: .75rem; color: var(--text-muted); margin-bottom: 2px; }
  .guide-footer .footer-card .fc-title { font-size: .95rem; font-weight: 600; color: var(--text); }
  .guide-footer .footer-card.next { text-align: right; }
  .guide-footer .help-section { text-align: center; padding: 28px; background: var(--bg-card); border: 1px solid var(--border); border-radius: var(--radius-lg); }
  .guide-footer .help-section h3 { margin-bottom: 12px; font-size: 1.1rem; }
  .guide-footer .help-links { display: flex; gap: 12px; justify-content: center; flex-wrap: wrap; }
  .guide-footer .help-link { display: inline-flex; align-items: center; gap: 6px; padding: 8px 18px; border-radius: 20px; border: 1px solid var(--border); color: var(--text-secondary); text-decoration: none; font-size: .85rem; transition: var(--transition); }
  .guide-footer .help-link:hover { border-color: var(--primary); color: var(--primary-light); background: rgba(99,102,241,.1); }

  /* ─── Animations ─── */
  @keyframes fadeInUp { from { opacity: 0; transform: translateY(20px); } to { opacity: 1; transform: translateY(0); } }
  .animate-in { animation: fadeInUp .6s ease forwards; opacity: 0; }
  .delay-1 { animation-delay: .1s; }
  .delay-2 { animation-delay: .2s; }
  .delay-3 { animation-delay: .3s; }
  .delay-4 { animation-delay: .4s; }

  /* ─── Mobile Sidebar Toggle ─── */
  .sidebar-toggle { display: none; position: fixed; bottom: 24px; right: 24px; width: 48px; height: 48px; border-radius: 50%; background: var(--primary); color: #fff; border: none; cursor: pointer; font-size: 1.3rem; z-index: 200; box-shadow: 0 4px 20px rgba(99,102,241,.4); transition: var(--transition); }
  .sidebar-toggle:hover { background: var(--primary-dark); transform: scale(1.05); }

  /* ─── Responsive ─── */
  @media (max-width: 1024px) {
    .sidebar { position: fixed; left: -280px; z-index: 150; height: 100vh; top: 0; transition: left var(--transition); background: var(--bg); padding-top: 80px; }
    .sidebar.open { left: 0; }
    .sidebar-toggle { display: flex; align-items: center; justify-content: center; }
    .main { padding: 32px 24px 60px; }
    .cards-grid { grid-template-columns: 1fr; }
    .guide-footer .footer-nav { grid-template-columns: 1fr; }
  }
  @media (max-width: 640px) {
    .main { padding: 24px 16px 48px; }
    .hero h1 { font-size: 1.6rem; }
    .step-section .step-header { padding: 16px 20px; }
    .step-section .step-content { padding: 20px; }
    .lightbox .lb-nav { width: 36px; height: 36px; font-size: 1rem; }
    .lightbox { padding: 16px; }
  }
`}</style>

<div className="guide-root">
  <div className="bg-grid"></div>
  <div className="bg-glow g1"></div>
  <div className="bg-glow g2"></div>
  <div className="bg-glow g3"></div>

  {/* ─── Lightbox ─── */}
  <div className="lightbox" id="lightbox" onClick="closeLightbox()">
    <button className="lb-close" onClick="closeLightbox()" aria-label="关闭">✕</button>
    <button className="lb-nav lb-prev" onClick="event.stopPropagation();navLightbox(-1)" aria-label="上一张">◀</button>
    <button className="lb-nav lb-next" onClick="event.stopPropagation();navLightbox(1)" aria-label="下一张">▶</button>
    <img src="" alt="" id="lightbox-img" />
    <div className="lb-counter" id="lb-counter"></div>
  </div>

  {/* ─── Layout ─── */}
  <div className="layout">

    {/* ─── Sidebar ─── */}
    <aside className="sidebar" id="sidebar">
      <h3>📑 目录导航</h3>
      <ul className="toc">
        <li><a href="#hero" className="active">🚀 概览</a></li>
        <li><a href="#step1">1️⃣ 注册 & 充值</a></li>
        <li><a href="#step2">2️⃣ 创建 API Key & 调用模型</a></li>
        <li><a href="#step2-1" className="sub">— 创建 API Key</a></li>
        <li><a href="#step2-2" className="sub">— 浏览模型市场</a></li>
        <li><a href="#step2-3" className="sub">— API 调用示例</a></li>
        <li><a href="#step2-4" className="sub">— Playground 调试</a></li>
        <li><a href="#step3">3️⃣ 日志 & 用量统计</a></li>
        <li><a href="#help">💬 帮助与支持</a></li>
      </ul>
      <div className="progress-ring">
        <div className="ring-label">阅读进度</div>
        <div className="ring-value" id="progress-text">0%</div>
        <div className="progress-track">
          <div className="progress-fill" id="progress-fill" style={{ width: '0%' }}></div>
        </div>
      </div>
    </aside>

    {/* ─── Mobile Sidebar Toggle ─── */}
    <button className="sidebar-toggle" onClick="toggleSidebar()" aria-label="菜单">☰</button>

    {/* ─── Main Content ─── */}
    <main className="main">

      {/* ═══ HERO ═══ */}
      <section className="hero animate-in" id="hero">
        <div className="badge">
          <span className="dot"></span> 快速入门指南 · 预计 10 分钟
        </div>
        <h1>PowerTokens<br />新用户快速入门</h1>
        <p className="subtitle">
          PowerTokens 为生成式 AI 应用提供<strong>统一接口</strong>。你可以通过同一个平台接入聊天、图像、视频和音频模型，并在控制台管理 Credits、API Key、调用日志和用量统计。
        </p>
      </section>

      {/* ═══ 任务清单概览 ═══ */}
      <div className="cards-grid animate-in delay-1">
        <div className="card">
          <div className="card-icon primary">👤</div>
          <h4>1. 注册账号并充值 Credits</h4>
          <p>通过 Google、GitHub 或邮箱注册，进入 Dashboard 完成首次充值。</p>
          <span className="tag billing">Credits</span>
          <span className="tag api">PayPal</span>
        </div>
        <div className="card">
          <div className="card-icon accent">🔑</div>
          <h4>2. 创建 API Key 并调用模型</h4>
          <p>创建 API Key，配置额度与权限，通过兼容接口调用模型。</p>
          <span className="tag api">API</span>
          <span className="tag model">模型</span>
        </div>
        <div className="card">
          <div className="card-icon success">📊</div>
          <h4>3. 查看日志和用量统计</h4>
          <p>通过 Logs 和 Dashboard 追踪调用结果、排查错误、控制成本。</p>
          <span className="tag api">日志</span>
          <span className="tag billing">用量</span>
        </div>
      </div>

      {/* ═══ STEP 1: 注册账号并充值 Credits ═══ */}
      <div className="step-section" id="step1">
        <div className="step-header" onClick="toggleStep(this)">
          <div className="step-number">1</div>
          <div className="step-info">
            <h2>注册账号并充值 Credits</h2>
            <p>创建 PowerTokens 账户，完成首次充值，获取 API 调用额度</p>
          </div>
          <div className="step-arrow">▼</div>
        </div>
        <div className="step-body">
          <div className="step-content">

            <div className="cards-grid">
              <div className="card">
                <div className="card-icon primary">🌐</div>
                <h4>访问官网注册</h4>
                <p>前往 <a href="https://powertokens.ai" target="_blank" style={{ color: 'var(--primary-light)' }}>PowerTokens 官网</a> 创建账号。支持 <strong>Google 登录</strong>、<strong>GitHub 登录</strong> 和 <strong>邮箱注册</strong> 三种方式。</p>
              </div>
              <div className="card">
                <div className="card-icon accent">💰</div>
                <h4>进入 Billing 页面</h4>
                <p>注册完成后进入 <strong>Dashboard 控制台</strong>，打开 <strong>Billing</strong> 页面选择适合你的购买套餐。</p>
              </div>
              <div className="card">
                <div className="card-icon success">💳</div>
                <h4>完成支付</h4>
                <p>支持 <strong>信用卡</strong> 和 <strong>PayPal</strong> 两种支付方式。Credits 是你的账户余额，调用模型时按用量自动扣费。</p>
              </div>
            </div>

            {/* 📸 Billing 截图 */}
            <figure className="screenshot-wrap" onClick="event.stopPropagation();openLightbox(0)">
              <img
                className="screenshot-img"
                src="https://mintcdn.com/limeng/eG-MIjQm-sRke4-z/images/guides/powertokens-quickstart/billing.png?fit=max&auto=format&n=eG-MIjQm-sRke4-z&q=85&s=823267ea233b777624128a1cc7091b46"
                alt="PowerTokens Billing 充值页面截图"
                loading="lazy"
              />
              <div className="screenshot-overlay"><span className="zoom-icon">🔍</span></div>
            </figure>
            <div className="screenshot-caption">
              <span className="caption-dot"></span> PowerTokens Billing 页面 — 选择套餐并完成充值
            </div>

            <div className="info-box tip">
              <span className="info-icon">💡</span>
              <div>
                <strong>什么是 Credits？</strong><br />
                Credits 是 PowerTokens 平台的账户余额。每次调用模型时，平台会根据模型用量自动从 Credits 中扣费。不同模型的费率不同，请在 Models 页面查看具体定价。
              </div>
            </div>

          </div>
        </div>
      </div>

      {/* ═══ STEP 2: 创建 API Key 并调用模型 ═══ */}
      <div className="step-section" id="step2">
        <div className="step-header" onClick="toggleStep(this)">
          <div className="step-number">2</div>
          <div className="step-info">
            <h2>创建 API Key 并调用模型</h2>
            <p>创建 API Key，浏览模型市场，通过 API 或 Playground 发起第一次请求</p>
          </div>
          <div className="step-arrow">▼</div>
        </div>
        <div className="step-body">
          <div className="step-content">

            <div className="timeline">
              <div className="tl-item">
                <h4>创建并保存 API Key</h4>
                <p>在 API Keys 页面创建密钥，配置额度、重置周期和过期时间</p>
              </div>
              <div className="tl-item">
                <h4>浏览模型市场</h4>
                <p>在 Models 页面选择 LLM、图像、视频或音频模型</p>
              </div>
              <div className="tl-item">
                <h4>发起 API 调用</h4>
                <p>使用 OpenAI 兼容接口，修改 Base URL 即可无缝接入</p>
              </div>
            </div>

            {/* ── 2.1 创建 API Key ── */}
            <h3 id="step2-1" style={{ marginTop: 32, fontSize: '1.1rem', fontWeight: 700 }}>
              📝 创建 API Key
            </h3>
            <p style={{ color: 'var(--text-secondary)', margin: '8px 0 16px' }}>
              在 <strong>API Keys</strong> 页面点击右上角 <strong>Create new key</strong> 创建新的 API Key。
            </p>

            {/* 📸 API Keys 列表截图 */}
            <figure className="screenshot-wrap" onClick="event.stopPropagation();openLightbox(1)">
              <img className="screenshot-img" src="https://mintcdn.com/limeng/eG-MIjQm-sRke4-z/images/guides/powertokens-quickstart/api-keys.png?fit=max&auto=format&n=eG-MIjQm-sRke4-z&q=85&s=52c9167e3aa320c38026aefa36dbf1c6" alt="PowerTokens API Keys 管理页面" loading="lazy" />
              <div className="screenshot-overlay"><span className="zoom-icon">🔍</span></div>
            </figure>
            <div className="screenshot-caption">
              <span className="caption-dot"></span> API Keys 管理页面 — 查看、创建和管理你的密钥
            </div>

            <p style={{ color: 'var(--text-secondary)', margin: '16px 0' }}>
              创建密钥时，你可以配置以下选项：
            </p>

            <table className="styled-table">
              <thead>
                <tr>
                  <th>配置项</th>
                  <th>说明</th>
                  <th>推荐做法</th>
                </tr>
              </thead>
              <tbody>
                <tr>
                  <td><strong>密钥名称</strong></td>
                  <td>为 Key 设置一个清晰的名字</td>
                  <td>例如 <code style={{ background: 'rgba(99,102,241,.2)', padding: '1px 6px', borderRadius: 3, color: 'var(--primary-light)' }}>LLM-key</code>，便于按项目管理</td>
                </tr>
                <tr>
                  <td><strong>用量限制</strong></td>
                  <td>设置固定 Credits 上限或无限制</td>
                  <td>建议设置上限以控制预算，额度耗尽后该 Key 自动停止</td>
                </tr>
                <tr>
                  <td><strong>重置类型</strong></td>
                  <td>额度重置周期</td>
                  <td>可选：不重置 / 每日 / 每周 / 每月（基于 UTC 时间）</td>
                </tr>
                <tr>
                  <td><strong>过期时间</strong></td>
                  <td>密钥自动失效时间</td>
                  <td>可选：不过期 / 7 天 / 30 天，生产环境建议定期轮换</td>
                </tr>
              </tbody>
            </table>

            {/* 📸 Create API Key 截图 */}
            <figure className="screenshot-wrap" onClick="event.stopPropagation();openLightbox(2)">
              <img className="screenshot-img" src="https://mintcdn.com/limeng/eG-MIjQm-sRke4-z/images/guides/powertokens-quickstart/create-api-key.png?fit=max&auto=format&n=eG-MIjQm-sRke4-z&q=85&s=c7c43bb034be2f4b4d20b5e8c1dcf896" alt="PowerTokens 创建 API Key 表单" loading="lazy" />
              <div className="screenshot-overlay"><span className="zoom-icon">🔍</span></div>
            </figure>
            <div className="screenshot-caption">
              <span className="caption-dot"></span> 创建 API Key 表单 — 配置名称、额度、重置周期和过期时间
            </div>

            <div className="info-box danger">
              <span className="info-icon">⚠️</span>
              <div>
                <strong>安全警告</strong><br />
                API Key <strong>只会在创建时展示一次</strong>！请立即妥善保存，不要将 Key 暴露在客户端代码、公开仓库或聊天记录中。如不慎泄露，请立即删除该 Key 并重新创建。
              </div>
            </div>

            {/* ── 2.2 浏览模型市场 ── */}
            <h3 id="step2-2" style={{ marginTop: 32, fontSize: '1.1rem', fontWeight: 700 }}>
              🔍 浏览模型市场
            </h3>
            <p style={{ color: 'var(--text-secondary)', margin: '8px 0 16px' }}>
              进入 <strong>Models</strong> 页面，你可以按类别浏览所有可用模型：
            </p>

            <div className="cards-grid">
              <div className="card">
                <div className="card-icon primary">💬</div>
                <h4>LLM 语言模型</h4>
                <p>大语言模型，支持对话补全、文本生成等任务</p>
                <span className="tag model">Chat Completion</span>
              </div>
              <div className="card">
                <div className="card-icon accent">🖼️</div>
                <h4>图像模型</h4>
                <p>文生图、图生图、图像编辑等视觉生成能力</p>
                <span className="tag model">Image Generation</span>
              </div>
              <div className="card">
                <div className="card-icon success">🎥</div>
                <h4>视频模型</h4>
                <p>文生视频、图生视频等视频生成能力</p>
                <span className="tag model">Video Generation</span>
              </div>
              <div className="card">
                <div className="card-icon warning">🎵</div>
                <h4>音频模型</h4>
                <p>语音合成、音乐生成等音频处理能力</p>
                <span className="tag model">Audio</span>
              </div>
            </div>

            {/* 📸 Models 市场截图 */}
            <figure className="screenshot-wrap" onClick="event.stopPropagation();openLightbox(3)">
              <img className="screenshot-img" src="https://mintcdn.com/limeng/eG-MIjQm-sRke4-z/images/guides/powertokens-quickstart/models.png?fit=max&auto=format&n=eG-MIjQm-sRke4-z&q=85&s=538fe2a57a55b45bc0036e65c7c8de93" alt="PowerTokens Models 模型市场页面" loading="lazy" />
              <div className="screenshot-overlay"><span className="zoom-icon">🔍</span></div>
            </figure>
            <div className="screenshot-caption">
              <span className="caption-dot"></span> Models 模型市场 — 浏览 LLM、图像、视频和音频模型
            </div>

            <div className="info-box info">
              <span className="info-icon">ℹ️</span>
              <div>
                <strong>关于模型接入</strong><br />
                模型详情页会展示<strong>模型能力、价格和可用的调用方式</strong>。MiniMax 等模型以 PowerTokens 上游渠道能力的形式对外提供，你通过平台统一接口发起请求，无需逐个对接。
              </div>
            </div>

            {/* ── 2.3 API 调用示例 ── */}
            <h3 id="step2-3" style={{ marginTop: 32, fontSize: '1.1rem', fontWeight: 700 }}>
              ⚡ 使用 API 调用模型
            </h3>
            <p style={{ color: 'var(--text-secondary)', margin: '8px 0 16px' }}>
              PowerTokens 提供 <strong>OpenAI 兼容接口</strong>。大多数 OpenAI SDK 只需修改 Base URL 并使用你创建的 API Key 即可接入。
            </p>

            <table className="styled-table" style={{ maxWidth: 600 }}>
              <tbody>
                <tr>
                  <td><strong>Base URL</strong></td>
                  <td><code style={{ background: 'rgba(99,102,241,.2)', padding: '2px 8px', borderRadius: 4, color: '#7ee787', fontFamily: "'JetBrains Mono', monospace" }}>https://baze-api.powerbuyin.top</code></td>
                </tr>
                <tr>
                  <td><strong>认证头</strong></td>
                  <td><code style={{ background: 'rgba(99,102,241,.2)', padding: '2px 8px', borderRadius: 4, color: '#a5d6ff', fontFamily: "'JetBrains Mono', monospace" }}>Authorization: Bearer &lt;token&gt;</code></td>
                </tr>
                <tr>
                  <td><strong>Content-Type</strong></td>
                  <td><code style={{ background: 'rgba(99,102,241,.2)', padding: '2px 8px', borderRadius: 4, color: '#a5d6ff', fontFamily: "'JetBrains Mono', monospace" }}>application/json</code></td>
                </tr>
              </tbody>
            </table>

            <p style={{ margin: '16px 0 8px', fontWeight: 600, fontSize: '.9rem', color: 'var(--text)' }}>📋 cURL 示例请求：</p>

            <div className="code-block-wrapper">
              <div className="code-header">
                <span className="lang-tag">bash</span>
                <button className="copy-btn" onClick="copyCode(this, 'curl-code')">📋 复制</button>
              </div>
              <pre id="curl-code" dangerouslySetInnerHTML={{ __html: `<span class="kw">curl</span> <span class="op">\\</span>
  <span class="op">--request</span> POST <span class="op">\\</span>
  <span class="op">--url</span> <span class="url">https://baze-api.powerbuyin.top/v1/chat/completions</span> <span class="op">\\</span>
  <span class="op">--header</span> <span class="str">'Authorization: Bearer &lt;token&gt;'</span> <span class="op">\\</span>
  <span class="op">--header</span> <span class="str">'Content-Type: application/json'</span> <span class="op">\\</span>
  <span class="op">--data</span> <span class="str">'{
  "model": "MiniMax-M3",
  "messages": [
    {"role": "system", "content": "You are a concise assistant."},
    {"role": "user", "content": "Describe Paris in one sentence."}
  ],
  "temperature": 0.3
}'</span>` }} /></pre>
            </div>

            <div className="info-box warning-box">
              <span className="info-icon">📌</span>
              <div>
                <strong>注意事项</strong><br />
                在发起正式接入前，请根据目标模型页面确认<strong>可用参数、请求字段和返回格式</strong>。不同模型支持的参数可能有所不同。
              </div>
            </div>

            {/* ── 2.4 Playground 调试 ── */}
            <h3 id="step2-4" style={{ marginTop: 32, fontSize: '1.1rem', fontWeight: 700 }}>
              🎮 使用 Playground 调试模型
            </h3>
            <p style={{ color: 'var(--text-secondary)', margin: '8px 0 16px' }}>
              进入 <strong>Models</strong> 页面并点击模型进入详情页，在 <strong>Playground</strong> 中直接测试模型能力，无需先编写代码。
            </p>

            <div className="cards-grid">
              <div className="card">
                <div className="card-icon primary">✏️</div>
                <h4>调试 Prompt</h4>
                <p>实时调整系统提示词，观察模型输出变化</p>
              </div>
              <div className="card">
                <div className="card-icon accent">🔄</div>
                <h4>对比模型效果</h4>
                <p>在同一个界面中切换不同模型，快速对比效果</p>
              </div>
              <div className="card">
                <div className="card-icon success">🎛️</div>
                <h4>测试参数</h4>
                <p>调整 temperature、top_p 等参数，找到最佳配置</p>
              </div>
              <div className="card">
                <div className="card-icon warning">🚀</div>
                <h4>验证原型</h4>
                <p>在编码前快速验证模型是否能满足需求</p>
              </div>
            </div>

            {/* 📸 Playground 截图 */}
            <figure className="screenshot-wrap" onClick="event.stopPropagation();openLightbox(4)">
              <img className="screenshot-img" src="https://mintcdn.com/limeng/eG-MIjQm-sRke4-z/images/guides/powertokens-quickstart/playground.png?fit=max&auto=format&n=eG-MIjQm-sRke4-z&q=85&s=92f35ed4aa49eef98702d425f085dad9" alt="PowerTokens Playground 调试界面" loading="lazy" />
              <div className="screenshot-overlay"><span className="zoom-icon">🔍</span></div>
            </figure>
            <div className="screenshot-caption">
              <span className="caption-dot"></span> Playground — 无需编写代码即可调试 Prompt 和参数
            </div>

          </div>
        </div>
      </div>

      {/* ═══ STEP 3: 查看日志和用量统计 ═══ */}
      <div className="step-section" id="step3">
        <div className="step-header" onClick="toggleStep(this)">
          <div className="step-number">3</div>
          <div className="step-info">
            <h2>查看日志和用量统计</h2>
            <p>使用 Logs 和 Dashboard 追踪请求、排查错误并控制 Credits 消耗</p>
          </div>
          <div className="step-arrow">▼</div>
        </div>
        <div className="step-body">
          <div className="step-content">

            <p style={{ color: 'var(--text-secondary)', marginBottom: 20 }}>
              发起模型请求后，你可以在 <strong>Logs</strong> 和 <strong>Dashboard</strong> 中查看调用结果、分析异常并控制成本。
            </p>

            <div className="cards-grid">
              <div className="card">
                <div className="card-icon primary">📋</div>
                <h4>调用日志 (Logs)</h4>
                <p>查看 API 请求的完整记录，包括时间、模型、参数和返回内容。适合排查单次调用的具体问题。</p>
              </div>
              <div className="card">
                <div className="card-icon accent">📊</div>
                <h4>用量统计 (Dashboard)</h4>
                <p>从模型或 API Key 维度查看 Credits 消耗、请求次数、成功率等聚合数据。适合控制成本和业务分析。</p>
              </div>
            </div>

            {/* ── 3.1 查看调用日志 ── */}
            <h3 style={{ marginTop: 28, fontSize: '1.1rem', fontWeight: 700 }}>
              📋 查看调用日志
            </h3>
            <p style={{ color: 'var(--text-secondary)', margin: '8px 0 16px' }}>
              进入 <strong>Logs</strong> 页面查看 API 请求记录。日志中心提供以下功能：
            </p>

            <table className="styled-table">
              <thead>
                <tr>
                  <th>功能</th>
                  <th>说明</th>
                </tr>
              </thead>
              <tbody>
                <tr>
                  <td>🔍 <strong>查看请求记录</strong></td>
                  <td>按时间顺序展示所有 API 请求的完整记录</td>
                </tr>
                <tr>
                  <td>📤 <strong>追踪生成结果</strong></td>
                  <td>查看每次调用的模型输出内容</td>
                </tr>
                <tr>
                  <td>⚠️ <strong>定位调用异常</strong></td>
                  <td>快速发现失败的请求和异常响应</td>
                </tr>
                <tr>
                  <td>🐛 <strong>分析错误信息</strong></td>
                  <td>查看详细的错误码和错误描述</td>
                </tr>
                <tr>
                  <td>🔧 <strong>调试请求参数</strong></td>
                  <td>回顾每次请求的参数设置</td>
                </tr>
              </tbody>
            </table>

            {/* 📸 Logs 截图 */}
            <figure className="screenshot-wrap" onClick="event.stopPropagation();openLightbox(5)">
              <img className="screenshot-img" src="https://mintcdn.com/limeng/eG-MIjQm-sRke4-z/images/guides/powertokens-quickstart/logs.png?fit=max&auto=format&n=eG-MIjQm-sRke4-z&q=85&s=196792cb1b6b2303798651f0b6951f49" alt="PowerTokens Logs 调用日志页面" loading="lazy" />
              <div className="screenshot-overlay"><span className="zoom-icon">🔍</span></div>
            </figure>
            <div className="screenshot-caption">
              <span className="caption-dot"></span> Logs 页面 — 查看请求时间、模型名称、参数和错误信息
            </div>

            <div className="info-box tip">
              <span className="info-icon">💡</span>
              <div>
                <strong>排查技巧</strong><br />
                排查问题时，先确认 <strong>请求时间</strong>、<strong>模型名称</strong>、<strong>请求参数</strong> 和 <strong>错误信息</strong> 是否符合预期。这四个维度通常能快速定位问题根因。
              </div>
            </div>

            {/* ── 3.2 查看用量统计 ── */}
            <h3 style={{ marginTop: 28, fontSize: '1.1rem', fontWeight: 700 }}>
              📊 查看用量统计
            </h3>
            <p style={{ color: 'var(--text-secondary)', margin: '8px 0 16px' }}>
              进入 <strong>Dashboard</strong> 页面查看资源消耗情况。你可以从两个维度分析用量数据：
            </p>

            <div className="cards-grid">
              <div className="card">
                <div className="card-icon primary">🤖</div>
                <h4>按模型维度</h4>
                <p>查看每种模型的 Credits 消耗、请求次数和成功率，优化模型选择</p>
              </div>
              <div className="card">
                <div className="card-icon accent">🔑</div>
                <h4>按 API Key 维度</h4>
                <p>查看每个 Key 的用量情况和消耗，便于按项目管理成本</p>
              </div>
            </div>

            <table className="styled-table">
              <thead>
                <tr>
                  <th>监控指标</th>
                  <th>说明</th>
                  <th>用途</th>
                </tr>
              </thead>
              <tbody>
                <tr>
                  <td><span className="tag billing">Credits 消耗</span></td>
                  <td>账户余额的消费情况</td>
                  <td>预算控制</td>
                </tr>
                <tr>
                  <td><span className="tag api">请求次数</span></td>
                  <td>API 调用总量</td>
                  <td>流量分析</td>
                </tr>
                <tr>
                  <td><span className="tag model">成功率</span></td>
                  <td>请求成功占总请求的比例</td>
                  <td>质量监控</td>
                </tr>
                <tr>
                  <td><span className="tag security">Key 消耗情况</span></td>
                  <td>每个 API Key 的单独消耗</td>
                  <td>项目管理</td>
                </tr>
              </tbody>
            </table>

            {/* 📸 Dashboard 截图 */}
            <figure className="screenshot-wrap" onClick="event.stopPropagation();openLightbox(6)">
              <img className="screenshot-img" src="https://mintcdn.com/limeng/eG-MIjQm-sRke4-z/images/guides/powertokens-quickstart/dashboard.png?fit=max&auto=format&n=eG-MIjQm-sRke4-z&q=85&s=c73d6636e35e04304e4c4e21400f123d" alt="PowerTokens Dashboard 用量统计页面" loading="lazy" />
              <div className="screenshot-overlay"><span className="zoom-icon">🔍</span></div>
            </figure>
            <div className="screenshot-caption">
              <span className="caption-dot"></span> Dashboard — 按模型和 Key 维度查看 Credits 消耗与请求统计
            </div>

          </div>
        </div>
      </div>

      {/* ═══ 帮助与支持 ═══ */}
      <div className="guide-footer" id="help">
        <div className="footer-nav">
          <a href="/zh-Hans/guides/powertokens-quickstart" className="footer-card prev">
            <span className="fc-arrow">←</span>
            <div className="fc-info">
              <div className="fc-label">上一篇</div>
              <div className="fc-title">PowerTokens 快速入门</div>
            </div>
          </a>
          <a href="/zh-Hans/guides/api-key-and-model-call" className="footer-card next">
            <div className="fc-info" style={{ textAlign: 'right' }}>
              <div className="fc-label">下一篇</div>
              <div className="fc-title">创建 API Key 并调用模型</div>
            </div>
            <span className="fc-arrow">→</span>
          </a>
        </div>

        <div className="help-section">
          <h3>💬 获取帮助与支持</h3>
          <p style={{ color: 'var(--text-secondary)', marginBottom: 16, fontSize: '.9rem' }}>
            如果你在使用过程中遇到问题，可以通过以下方式联系我们：
          </p>
          <div className="help-links">
            <a href="mailto:service@powertokens.ai" className="help-link">📧 service@powertokens.ai</a>
            <a href="https://discord.gg/bWA4EaFQ4" target="_blank" className="help-link">💬 Discord 社区</a>
            <a href="https://docs.powertokens.ai" target="_blank" className="help-link">📚 完整文档</a>
          </div>
        </div>
      </div>

    </main>
  </div>
</div>

<script dangerouslySetInnerHTML={{ __html: `
  // ─── Screenshot Gallery Data ───
  const gallery = [
    { src: 'https://mintcdn.com/limeng/eG-MIjQm-sRke4-z/images/guides/powertokens-quickstart/billing.png?fit=max&auto=format&n=eG-MIjQm-sRke4-z&q=85&s=823267ea233b777624128a1cc7091b46', alt: 'Billing 充值页面', caption: 'Billing 页面 — 选择套餐并完成充值' },
    { src: 'https://mintcdn.com/limeng/eG-MIjQm-sRke4-z/images/guides/powertokens-quickstart/api-keys.png?fit=max&auto=format&n=eG-MIjQm-sRke4-z&q=85&s=52c9167e3aa320c38026aefa36dbf1c6', alt: 'API Keys 管理页面', caption: 'API Keys 管理页面 — 查看、创建和管理你的密钥' },
    { src: 'https://mintcdn.com/limeng/eG-MIjQm-sRke4-z/images/guides/powertokens-quickstart/create-api-key.png?fit=max&auto=format&n=eG-MIjQm-sRke4-z&q=85&s=c7c43bb034be2f4b4d20b5e8c1dcf896', alt: '创建 API Key 表单', caption: '创建 API Key 表单 — 配置名称、额度、重置周期和过期时间' },
    { src: 'https://mintcdn.com/limeng/eG-MIjQm-sRke4-z/images/guides/powertokens-quickstart/models.png?fit=max&auto=format&n=eG-MIjQm-sRke4-z&q=85&s=538fe2a57a55b45bc0036e65c7c8de93', alt: 'Models 模型市场', caption: 'Models 模型市场 — 浏览 LLM、图像、视频和音频模型' },
    { src: 'https://mintcdn.com/limeng/eG-MIjQm-sRke4-z/images/guides/powertokens-quickstart/playground.png?fit=max&auto=format&n=eG-MIjQm-sRke4-z&q=85&s=92f35ed4aa49eef98702d425f085dad9', alt: 'Playground 调试界面', caption: 'Playground — 无需编写代码即可调试 Prompt 和参数' },
    { src: 'https://mintcdn.com/limeng/eG-MIjQm-sRke4-z/images/guides/powertokens-quickstart/logs.png?fit=max&auto=format&n=eG-MIjQm-sRke4-z&q=85&s=196792cb1b6b2303798651f0b6951f49', alt: 'Logs 调用日志页面', caption: 'Logs 页面 — 查看请求时间、模型名称、参数和错误信息' },
    { src: 'https://mintcdn.com/limeng/eG-MIjQm-sRke4-z/images/guides/powertokens-quickstart/dashboard.png?fit=max&auto=format&n=eG-MIjQm-sRke4-z&q=85&s=c73d6636e35e04304e4c4e21400f123d', alt: 'Dashboard 用量统计页面', caption: 'Dashboard — 按模型和 Key 维度查看 Credits 消耗与请求统计' }
  ];
  let currentIndex = 0;

  function openLightbox(index) {
    currentIndex = index;
    const lb = document.getElementById('lightbox');
    const img = document.getElementById('lightbox-img');
    const counter = document.getElementById('lb-counter');
    img.src = gallery[index].src;
    img.alt = gallery[index].alt;
    counter.textContent = (index + 1) + ' / ' + gallery.length + ' — ' + gallery[index].caption;
    lb.classList.add('open');
    document.body.style.overflow = 'hidden';
  }

  function closeLightbox() {
    document.getElementById('lightbox').classList.remove('open');
    document.body.style.overflow = '';
  }

  function navLightbox(dir) {
    currentIndex = (currentIndex + dir + gallery.length) % gallery.length;
    const img = document.getElementById('lightbox-img');
    const counter = document.getElementById('lb-counter');
    img.style.opacity = '0';
    setTimeout(() => {
      img.src = gallery[currentIndex].src;
      img.alt = gallery[currentIndex].alt;
      counter.textContent = (currentIndex + 1) + ' / ' + gallery.length + ' — ' + gallery[currentIndex].caption;
      img.style.opacity = '1';
    }, 150);
  }

  function toggleSidebar() { document.getElementById('sidebar').classList.toggle('open'); }

  function toggleStep(header) { header.parentElement.classList.toggle('open'); updateProgress(); }

  function copyCode(btn, codeId) {
    const pre = document.getElementById(codeId);
    navigator.clipboard.writeText(pre.innerText).then(() => {
      btn.textContent = '已复制';
      btn.classList.add('copied');
      setTimeout(() => { btn.textContent = '复制'; btn.classList.remove('copied'); }, 2000);
    });
  }

  function updateProgress() {
    const steps = document.querySelectorAll('.step-section');
    const openSteps = document.querySelectorAll('.step-section.open');
    const progress = Math.min(100, 10 + Math.round((openSteps.length / steps.length) * 90));
    document.getElementById('progress-text').textContent = progress + '%';
    document.getElementById('progress-fill').style.width = progress + '%';
  }

  const sections = document.querySelectorAll('[id]');
  const navLinks = document.querySelectorAll('.sidebar .toc a');
  window.addEventListener('scroll', () => {
    let current = '';
    sections.forEach(s => { if (scrollY >= s.offsetTop - 100) current = s.getAttribute('id'); });
    navLinks.forEach(l => { l.classList.remove('active'); if (l.getAttribute('href') === '#' + current) l.classList.add('active'); });
    updateProgress();
  });

  document.addEventListener('keydown', (e) => {
    const lb = document.getElementById('lightbox');
    if (lb.classList.contains('open')) {
      if (e.key === 'Escape') closeLightbox();
      if (e.key === 'ArrowLeft') navLightbox(-1);
      if (e.key === 'ArrowRight') navLightbox(1);
      return;
    }
    if (e.key === 'ArrowRight' && !e.target.closest('input, textarea')) {
      const closed = document.querySelector('.step-section:not(.open)');
      if (closed) { closed.classList.add('open'); closed.scrollIntoView({ behavior: 'smooth', block: 'start' }); updateProgress(); }
    }
    if (e.key === 'ArrowLeft' && !e.target.closest('input, textarea')) {
      const open = [...document.querySelectorAll('.step-section.open')].pop();
      if (open) { open.classList.remove('open'); updateProgress(); }
    }
  });

  updateProgress();
`}} />
